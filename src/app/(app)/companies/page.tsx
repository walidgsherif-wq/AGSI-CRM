import { PagePlaceholder } from '../_components/PagePlaceholder';

export default function CompaniesPage() {
  return (
    <PagePlaceholder
      title="Companies"
      milestone="M4"
      description="Canonical stakeholder master. List + detail tabs: Overview, Projects, Engagements, Tasks, Notes, Documents, Level History, Ownership Timeline."
    />
  );
}
