import { PagePlaceholder } from '../../../_components/PagePlaceholder';

export default function GeographicMapPage() {
  return (
    <PagePlaceholder
      title="Geographic heat map"
      milestone="M11"
      description="UAE choropleth at emirate + city level. Filter by company type, L-level, active-projects."
    />
  );
}
