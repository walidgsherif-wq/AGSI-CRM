import { PagePlaceholder } from '../_components/PagePlaceholder';

export default function InsightsPage() {
  return (
    <PagePlaceholder
      title="Insights"
      milestone="M14"
      description="Market insights powered by BNC snapshots — stage, city, sector, top-20 breakdowns. Snapshot picker + compare-to selector."
    />
  );
}
