import { PagePlaceholder } from '../../_components/PagePlaceholder';

export default function AdminSettingsPage() {
  return (
    <PagePlaceholder
      title="System settings"
      milestone="M8+"
      description="Stagnation rules, notification channels, FY config, universe sizes, composition thresholds, ecosystem scale, BEI weightings."
    />
  );
}
