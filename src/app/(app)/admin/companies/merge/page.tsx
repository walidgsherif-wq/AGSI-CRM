import { PagePlaceholder } from '../../../_components/PagePlaceholder';

export default function MatchQueuePage() {
  return (
    <PagePlaceholder
      title="Match queue"
      milestone="M5"
      description="Resolve 0.75–0.85 similarity matches from BNC uploads."
    />
  );
}
