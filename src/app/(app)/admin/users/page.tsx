import { PagePlaceholder } from '../../_components/PagePlaceholder';

export default function AdminUsersPage() {
  return (
    <PagePlaceholder
      title="Users"
      milestone="M3"
      description="Invite new users, change role, deactivate. Magic-link invite via Supabase."
    />
  );
}
