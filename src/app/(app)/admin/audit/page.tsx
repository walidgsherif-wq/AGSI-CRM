import { PagePlaceholder } from '../../_components/PagePlaceholder';

export default function AdminAuditPage() {
  return (
    <PagePlaceholder
      title="Audit log"
      milestone="M15"
      description="Filterable audit events. Admin-only read."
    />
  );
}
