import { PagePlaceholder } from '../../_components/PagePlaceholder';

export default function AdminTargetsPage() {
  return (
    <PagePlaceholder
      title="Targets"
      milestone="M8"
      description="Playbook defaults + per-member overrides. Bulk-apply multiplier."
    />
  );
}
