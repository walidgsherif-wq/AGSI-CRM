import { PagePlaceholder } from '../../_components/PagePlaceholder';

export default function AdminUploadsPage() {
  return (
    <PagePlaceholder
      title="BNC uploads"
      milestone="M5"
      description="Drag-drop BNC_Project_DDMMYYYY.xlsx. Parse → project resolver → company resolver → market snapshot."
    />
  );
}
