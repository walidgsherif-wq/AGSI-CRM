import { PagePlaceholder } from '../../_components/PagePlaceholder';

export default function AdminReportsPage() {
  return (
    <PagePlaceholder
      title="Report generation"
      milestone="M12"
      description="Create monthly/quarterly leadership reports. Draft → finalise → leadership feedback loop."
    />
  );
}
